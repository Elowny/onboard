# 横向过滤逻辑图示

## 场景说明

### 坐标系说明
- `boundary_l_min`: 路径左边界（车道左侧边界）
- `boundary_l_max`: 路径右边界（车道右侧边界）
- `kLateralEnterThres = 0.5m`: 横向进入阈值
- `object_frenet_box.l_min`: 物体左边界
- `object_frenet_box.l_max`: 物体右边界

---

## 情况1: 车辆完全在本车道内 ✅ 通过过滤

```
车道左边界                   车道右边界
boundary_l_min               boundary_l_max
    |                            |
    |  [========车辆========]    |  ← 车辆完全在车道内
    |                            |
    └────────────────────────────┘
    
条件检查:
- object.l_min >= boundary_l_min + 0.5 ✅
- object.l_max <= boundary_l_max - 0.5 ✅
结果: 通过过滤，继续检查是否阻塞
```

---

## 情况2: 车辆骑线行驶，但至少有0.5m在本车道内 ✅ 通过过滤

```
车道左边界                   车道右边界
boundary_l_min               boundary_l_max
    |                            |
    |  [====车辆====]             |  ← 车辆部分在车道内
    |         |                   |
    └─────────┴───────────────────┘
              ↑
        至少0.5m重叠
    
条件检查:
- object.l_min <= boundary_l_max - 0.5 ✅ (车辆左边界在边界内)
- object.l_max >= boundary_l_min + 0.5 ✅ (车辆右边界在边界内)
结果: 通过过滤，继续检查是否阻塞
```

---

## 情况3: 车辆大部分在相邻车道，只有很少部分(<0.5m)在本车道内 ❌ 被过滤

```
车道左边界                   车道右边界
boundary_l_min               boundary_l_max
    |                            |
    |                            |  [====车辆====]  ← 车辆主要在相邻车道
    |                            |     |
    └────────────────────────────┴─────┘
                                  ↑
                            重叠<0.5m
    
条件检查:
- object.l_min > boundary_l_max - 0.5 ❌ (车辆左边界在边界外)
结果: 被过滤，不会成为纵向目标
```

---

## 情况4: 车辆完全在相邻车道 ❌ 被过滤

```
车道左边界                   车道右边界
boundary_l_min               boundary_l_max
    |                            |
    |                            |      [====车辆====]  ← 完全在相邻车道
    |                            |
    └────────────────────────────┘
    
条件检查:
- object.l_min > boundary_l_max - 0.5 ❌
结果: 被过滤，不会成为纵向目标
```

---

## 完整处理流程

```
                   车辆检测
                      |
                      ↓
         ┌────────────────────────┐
         │ FilterObjectViaDrivePassage │
         │  横向过滤检查              │
         └────────────────────────┘
                      |
        ┌─────────────┴─────────────┐
        |                           |
    通过过滤                     被过滤
    (≥0.5m重叠)                 (<0.5m重叠)
        |                           |
        ↓                           ↓
   继续检查                     ❌ 结束
        |                    (不会成为纵向目标)
        ↓
┌───────────────────────────────┐
│ IsObjectBlockingRefCenter      │
│ 或 IsObjectBlockingEgoVehicle  │
│  阻塞判断                       │
└───────────────────────────────┘
        |
   ┌────┴────┐
   |         |
阻塞自车   不阻塞
   |         |
   ↓         ↓
✅ 成为纵向目标  ❌ 不成为纵向目标
```

---

## 关键代码逻辑

```cpp
// 第86-87行的过滤条件
if (object_frenet_box.l_min > boundary_l_max - kLateralEnterThres ||
    object_frenet_box.l_max < boundary_l_min + kLateralEnterThres) {
  // ❌ 被过滤：车辆在车道内的部分 < 0.5m
  return absl::OutOfRangeError("Object out of lateral boundary");
}
// ✅ 通过过滤：车辆在车道内的部分 ≥ 0.5m
// 继续后续的阻塞检查
```

---

## 总结

**正确理解：**
- ✅ 如果车辆骑线行驶，**只要至少有0.5m在本车道内**，就会通过过滤，继续检查是否阻塞
- ✅ 如果车辆阻塞自车路径，会被选为纵向目标
- ❌ 如果车辆在本车道内的部分 < 0.5m，会被过滤，不会成为纵向目标

**错误理解（需要纠正）：**
- ❌ "入侵不超过0.5m就不会被选为纵向目标" ← 这是错误的
- ✅ 正确：**入侵至少0.5m才会继续检查，才有可能成为纵向目标**
