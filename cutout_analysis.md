# 车辆切出时纵向目标释放机制分析

## 问题描述

当其他车辆切出（cut-out）本车道时，代码是否会在确保无碰撞的情况下才释放这个纵向目标？

## 当前代码逻辑分析

### 1. 纵向目标的判断流程

```
每个规划周期 (FindLeadingObjects)
    ↓
FilterObjectViaDrivePassage (横向过滤)
    ↓
检查: object.l_min > boundary_l_max - 0.5m 或
     object.l_max < boundary_l_min + 0.5m
    ↓
如果满足 → 被过滤掉（不会成为纵向目标）
如果不满足 → 继续检查是否阻塞
```

### 2. 关键代码位置

**文件**: `planner/decision/leading_object.cc`

**函数**: `FilterObjectViaDrivePassage` (第65-93行)

```cpp
constexpr double kLateralEnterThres = 0.5;  // m.
const auto [boundary_l_max, boundary_l_min] =
    CalcSlBoundaries(sl_boundary, object_frenet_box);
// Filter objects not on path boundary.
if (object_frenet_box.l_min > boundary_l_max - kLateralEnterThres ||
    object_frenet_box.l_max < boundary_l_min + kLateralEnterThres) {
  return absl::OutOfRangeError(...);  // 被过滤掉
}
```

### 3. 切出场景分析

#### 场景A: 车辆正在切出，但仍有部分在本车道内

```
时刻T1: 车辆在本车道内有0.6m → ✅ 通过过滤 → 成为纵向目标
时刻T2: 车辆在本车道内有0.4m → ❌ 被过滤 → 不再成为纵向目标
```

**问题**: 
- 在时刻T2，车辆虽然只有0.4m在本车道内，但可能仍然存在碰撞风险
- 代码仅基于**当前时刻**的横向位置判断，没有考虑：
  - 车辆的轨迹预测
  - 碰撞检查
  - 安全距离

#### 场景B: 车辆完全切出

```
时刻T1: 车辆在本车道内有0.6m → ✅ 通过过滤
时刻T2: 车辆完全切出（0m在本车道内）→ ❌ 被过滤
```

这种情况相对安全，因为车辆已经完全离开本车道。

## 潜在问题

### 问题1: 基于瞬时状态的判断

代码在每个规划周期都会重新计算 `FindLeadingObjects`，基于**当前时刻**的物体位置进行判断。如果车辆正在切出：

- **时刻T**: 车辆有0.6m在本车道内 → 成为纵向目标
- **时刻T+Δt**: 车辆只有0.3m在本车道内 → 被过滤掉

但在这两个时刻之间，车辆可能仍然存在碰撞风险。

### 问题2: 缺少碰撞检查

从代码来看，`FilterObjectViaDrivePassage` 只检查：
1. 纵向位置（是否在自车前方）
2. 横向位置（是否至少有0.5m在本车道内）

**没有检查**：
- 车辆的预测轨迹
- 是否存在碰撞风险
- 安全距离

### 问题3: 缺少时间维度的考虑

虽然 `CreateLeadingObject` 会生成 ST constraints（时空约束），但这些约束是基于：
- 静止物体：使用当前边界框，在整个时间范围内生成约束
- 运动物体：基于预测轨迹的时空状态生成约束

但是，**过滤逻辑本身**（`FilterObjectViaDrivePassage`）只考虑当前时刻的位置，不考虑未来时刻的碰撞风险。

## 建议的改进方向

### 1. 基于轨迹的过滤

不仅检查当前时刻的位置，还要检查预测轨迹：
- 如果车辆在未来一段时间内（例如2-3秒）仍然可能与本车存在碰撞风险，即使当前横向位置<0.5m，也应该继续作为纵向目标

### 2. 增加碰撞检查

在释放纵向目标之前，检查：
- 车辆的预测轨迹是否会在未来与自车轨迹相交
- 是否存在安全距离

### 3. 增加滞后机制（Hysteresis）

避免频繁切换：
- 当车辆从"成为纵向目标"变为"不成为纵向目标"时，增加一个滞后阈值
- 例如：成为目标需要≥0.5m，释放目标需要<0.3m（而不是0.5m）

## 代码验证建议

### 测试场景

1. **切出场景1**: 车辆从本车道切出，在切出过程中横向位置从0.6m变为0.4m
   - 验证：是否在0.4m时立即释放目标
   - 验证：是否存在碰撞风险

2. **切出场景2**: 车辆快速切出，在短时间内从0.6m变为0m
   - 验证：释放时机是否合理

3. **切出场景3**: 车辆切出后又切回
   - 验证：是否能及时重新识别为纵向目标

## 关键发现

### 过滤阶段 vs 约束生成阶段

代码中存在两个阶段：

1. **过滤阶段** (`FilterObjectViaDrivePassage`):
   - 使用 `st_traj.planner_object()` - **当前时刻**的物体状态
   - 基于当前时刻的横向位置（0.5m阈值）决定是否成为纵向目标

2. **约束生成阶段** (`CreateLeadingObject`):
   - 使用 `traj.states()` - **预测轨迹**的时空状态
   - 基于预测轨迹生成ST约束

**问题**：
- 过滤阶段只考虑当前时刻，不考虑预测轨迹
- 如果车辆正在切出，可能在下一个规划周期就被过滤掉，即使预测轨迹显示仍有碰撞风险

### 代码流程

```
每个规划周期:
  FindLeadingObjects()
    ↓
  FindFrontObjectsOnLane()
    ↓
  FilterObjectViaDrivePassage(st_traj.planner_object())  ← 使用当前时刻状态
    ↓
  检查: 当前横向位置是否 ≥ 0.5m
    ↓
  如果通过 → CreateLeadingObject()
    ↓
  生成ST约束 (基于 traj.states() 预测轨迹)  ← 使用预测轨迹
```

**关键问题**：
- 成为纵向目标的判断：基于**当前时刻**位置
- ST约束生成：基于**预测轨迹**
- 释放纵向目标：基于**下一个规划周期的当前时刻**位置

这可能导致：
- 车辆切出时，如果当前时刻位置<0.5m，立即被过滤
- 但预测轨迹可能显示未来仍有碰撞风险

## 结论

**当前代码逻辑存在问题**：

1. ✅ 每个规划周期都会重新计算，这是好的
2. ✅ ST约束基于预测轨迹生成，这是好的
3. ❌ **过滤阶段只基于当前时刻位置，没有考虑预测轨迹的碰撞风险**
4. ❌ **释放纵向目标时，没有检查预测轨迹是否仍有碰撞风险**

**具体问题场景**：

```
时刻T: 
  - 车辆当前位置: 0.6m在本车道内 → ✅ 成为纵向目标
  - 预测轨迹: 未来2秒内会切出，但仍可能碰撞

时刻T+Δt (下一个规划周期):
  - 车辆当前位置: 0.4m在本车道内 → ❌ 被过滤掉
  - 但预测轨迹可能显示: 未来1秒内仍有碰撞风险
```

**建议**：在释放纵向目标之前，应该：
1. 不仅检查当前时刻的横向位置
2. 还要检查预测轨迹在未来一段时间内（例如2-3秒）是否仍有碰撞风险
3. 只有当预测轨迹显示车辆已经完全切出且无碰撞风险时，才释放目标

否则，当车辆切出时，可能会过早释放目标，导致碰撞风险。
